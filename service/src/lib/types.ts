export interface Post {
  title: string;
  description: string;
  date: string;
  slug: string;
  draft: boolean;
}

export interface PostMetadata {
  title: string;
  description: string;
  date: string;
  draft: boolean;
}
